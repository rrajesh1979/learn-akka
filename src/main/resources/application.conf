akka.persistence.journal.plugin = "akka.persistence.journal.leveldb"
akka.persistence.journal.leveldb.dir = "target/payments/journal"
akka.actor.allow-java-serialization = true

alpakka {
  mongo {
    connection {
      ip = "localhost"
      port = 27017
    }
  }
}